# Config Editor

This project hosts the configuration editor tools and supporting documentation. Source scripts in scripts/ drive the UI and PDF exports, while ot-assets/ contains generated guides and reference PDFs.

Codex handshake verification — static test run.
