# New Project

This folder is reserved for an upcoming bot project. Keep executable drivers and automation scripts in `scripts/`, and store configuration files, credentials, reference data, or other artifacts in `bot-assets/`.

Add any project-specific notes here as the implementation takes shape.
