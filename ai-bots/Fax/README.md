# Fax Project

Contains fax automation tooling such as `send_irsfax.py`. Use `bot-assets/` for the data files, generated PDFs, and configs; place the drivers in `scripts/` and any documentation or reference materials in `source/`.
