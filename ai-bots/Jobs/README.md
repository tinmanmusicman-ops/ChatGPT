# Jobs Project

Processes job notification emails, extracts URLs, and logs hiring details to Google Sheets. Keep Gmail/API credentials in `bot-assets/config.json` and any supporting documents in `source/`.

Codex handshake verification — static test run.
