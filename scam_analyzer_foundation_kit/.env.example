# Copy this to .env and fill in the values (VS Code reads this during debug).
OPENAI_API_KEY=sk-REPLACE_ME
# You can override config.json values via environment if desired:
# GMAIL_USER=you@example.com
# GMAIL_APP_PASSWORD=your-16-char-app-password
