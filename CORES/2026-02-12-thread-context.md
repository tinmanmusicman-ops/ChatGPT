# Thread Context — February 12, 2026

- **Live call**: Anonymous “Call Tim Now — Live” button routes through `tower.py` via `/tower/live-call/token` and `/tower/live-call/call`, pulls Twilio REST credentials from `.gitignored EI_ControlTower/twillo.json`, and dials `DESTINATION_DEVICE` with the caller label “Website Visitor”. Localized `twilio.min.js` from `AI-Fit-Site/assets/js/` is loaded from multiple origin permutations plus a Cloudflare tunnel when available, and verbose client logging can be enabled by toggling `LIVE_CALL_VERBOSE_LOGGING`.
- **Call controls**: Added a cancel button that enables during an in-flight call (`liveCallInFlight`) and calls through `closeLiveCall`, logging `call.cancel.requested` and resetting both live-call buttons/status text.
- **Logging**: Live-call events go to `CORES/cores.log` via `writeLiveCallClientLog`; suppressed events still exist but can be exposed by re-enabling `LIVE_CALL_VERBOSE_LOGGING`. New AI logging hooks (`ai.request.debug`, `ai.response.debug`, `ai.request.error`) emit structured fields when `HSST_AI_DEBUG=1`, covering origin, mode, question/answer previews, and error details.
- **Deployment notes**: `EI_ControlTower/twillo.example.json` documents required keys and format to copy into `twillo.json`, `.gitignore` keeps the secrets out of git, and `node_modules/` plus Twilio SDK asset usage are handled locally.
